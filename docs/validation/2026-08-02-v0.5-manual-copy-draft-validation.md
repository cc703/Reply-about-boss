# v0.5 Manual Copy Draft Validation

## Summary

v0.5 adds a manual `复制` button for each generated reply draft.

The feature copies only the selected visible draft text to the clipboard after a user click. It does not fill BOSS inputs and does not send messages.

## Frontend Tests

Command:

```powershell
npm.cmd test
```

Result:

```text
pass 25
fail 0
```

New coverage:

- `copyDraftText` writes normalized draft text with an injected clipboard implementation.
- Empty draft text is rejected before clipboard write.
- Missing Clipboard API returns a controlled error.

## Syntax Check

Command:

```powershell
npm.cmd run check
```

Result: passed.

## Chrome Fixture Smoke

Method:

- Loaded the anonymized BOSS transcript fixture in Chrome headless.
- Injected the extension scripts.
- Simulated a successful draft state with two generated drafts.
- Parsed the rendered panel DOM.

Result:

```text
HasPanel                  : True
RecordRows                : 5
DraftItems                : 2
CopyButtons               : 2
HasDraftText              : True
RecordHasAssistantWelcome : False
RecordHasResumeAction     : False
StatusFound               : True
```

Interpretation:

- Draft items render.
- Each draft item has a copy button.
- Existing conversation filtering still excludes third-party assistant noise and `发简历`.

## Safety Scan

Commands scanned extension source for:

- BOSS input selectors,
- hidden textarea fallback,
- `execCommand`,
- programmatic `.click()`,
- `.submit()`,
- cookies,
- extension/local storage,
- extension runtime messaging.

Result: no matches.

Secret scan for real-looking API keys also returned no matches.

## Files Changed

- Created: `docs/superpowers/specs/2026-08-02-manual-copy-draft-design.md`
- Created: `docs/superpowers/plans/2026-08-02-manual-copy-draft-plan.md`
- Modified: `extension/src/replyDraftClient.js`
- Modified: `extension/tests/replyDraftClient.test.js`
- Modified: `extension/src/panel.js`
- Modified: `extension/src/content.js`
- Modified: `extension/styles/panel.css`
- Created: `docs/validation/2026-08-02-v0.5-manual-copy-draft-validation.md`

## Not Verified

- Real clipboard behavior inside a live logged-in BOSS page was not manually tested in this run.
- Real DeepSeek draft generation still depends on a local `DEEPSEEK_API_KEY`.

## Next Step

Run live QA with:

1. local backend started,
2. real `DEEPSEEK_API_KEY`,
3. unpacked extension reloaded,
4. BOSS chat page refreshed,
5. `生成回复草稿`,
6. `复制`,
7. manual paste by the user.
