# v1.0 Post-Publish Verification

## Scope

The first public source upload for `Reply-about-boss` was verified after the owner-authorized push.

## Evidence

- Repository: `https://github.com/cc703/Reply-about-boss`
- Branch: `main`
- Published source commit: `0dcd87bcb8ac8094aed71ff006676c98bfe3780c`
- Local `HEAD` equals `origin/main` after the push.
- Remote tracked tree contains 94 files.
- The remote tree contains `LICENSE`, `README.md`, `.gitignore`, extension source, backend source, tests, and project docs.
- The remote tree does not contain virtual environments, `.omx` runtime state, private documents, chat screenshots, or browser profile data.

## Privacy Scan

The tracked-file scan found zero hits for the checked patterns covering API-key prefixes, bearer tokens, private-key headers, email addresses, mainland mobile numbers, Windows user paths, `Miss.hm`, and temporary browser data paths.

The public source stores no DeepSeek API Key. The extension saves the user-provided Key through the local backend into Windows Credential Manager; the backend does not use an environment-variable or project-file Key fallback.

## Test Evidence

- `npm.cmd test`: 44 tests passed.
- `npm.cmd run check`: passed.
- Python backend unit tests: 53 tests passed.
- Python `compileall`: passed.
- `git diff --cached --check`: passed before the source commit.

## Remaining Risk

The backend permits the BOSS page and local Chrome extension origins required by the unpacked extension workflow. This is a local trust-boundary consideration, not a public credential exposure: the stored Key is never returned by the API.

## Result

The authorized v1.0 source package is published. No automatic message filling or sending is included.
